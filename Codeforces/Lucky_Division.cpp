#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <iostream>
#include <algorithm>

using namespace std;

#define L long int
#define LL long long int
#define ULL unsigned long long
#define MAX 100

/*int arr[] = {4, 7, 47, 74, 444, 777, 447, 477, 774, 747, 474, 744};

int main(){
    int n, i, j, cnt, four, seven, tmp, len;
    char str[MAX];
    while (scanf ("%d", &n) != EOF){

        if ((n%4) == 0 || (n%7) == 0 || (n%47) == 0 || (n%74) == 0 || (n%477) == 0) puts ("YES");
        else puts ("NO");
    }
    return 0;
}
*/

/*int main(){
    double a, b, root;
    scanf ("%lf%lf", &a, &b);
    root = sqrt (a+b);
    printf ("%lf\n", root);
    return 0;
}
*/
