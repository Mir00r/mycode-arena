#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <iostream>
#include <algorithm>

using namespace std;

#define l long int
#define ll long long int
#define MAX 9999

int main(){
    int tc, x_start, y_start, x_end, y_end, x_left, y_top, x_right, y_bottom;
    scanf ("%d", &tc);
    while (tc--){
        scanf ("%d%d%d%d%d%d%d%d", &x_start, &y_start, &x_end, &y_end, &x_left, &y_top, &x_right, &y_bottom);
        if ()
    }
    return 0;
}
