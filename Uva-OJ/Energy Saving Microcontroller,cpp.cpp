#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <iostream>
#include <algorithm>

using namespace std;

#define l long int
#define ll long long int
#define MAX 9999

int main(){
    int tc, cs=1, i, j, a, b;
    char str, ch;
    double res, tmp, weight;
    scanf ("%d", &tc);
    while (tc--){
        scanf ("%d%s%d%s", &a, str, &b, ch);
        if()
    }
    return 0;
}
